import React from 'react';
import styled from 'styled-components';
import type { Recent10GamesStatsTypes } from '../../StatsContainer';

export type RecentGamesSummaryPropTypes = {
  recent10GamesStats: Recent10GamesStatsTypes[];
};

export default function RecentGamesSummary({
  recent10GamesStats
}: RecentGamesSummaryPropTypes) {
  return <div></div>;
}
